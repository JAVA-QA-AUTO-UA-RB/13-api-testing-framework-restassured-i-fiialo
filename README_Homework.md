Відкрийте проєкт у IntelliJ IDEA.
Maven автоматично завантажить усі залежності з pom.xml Тести використовують публічний API: https://jsonplaceholder.typicode.com

Тому нічого додатково налаштовувати не потрібно — тести просто виконують запити до цього сервісу.

Як запускати тести локально
Усі тести написані за допомогою TestNG і мають теги (групи).

🔹 Запустити всі тести mvn test

🔹 Запустити лише smoke-тести mvn test -Dgroups=smoke

🔹 Запустити лише regression-тести mvn test -Dgroups=regression

У кожному тесті вказано групу через @Test(groups = "..."), а параметр -Dgroups=... дозволяє запустити лише потрібну групу тестів.

Як працюють тести на GitHub Actions
У репозиторії є файл .github/workflows/api-tests.yml і саме він відповідає за автоматичний запуск тестів після пушу.

🔹 Коли запускається

при пуші у гілку main

або при пуші у будь-яку гілку, що починається з ci_ (наприклад, ci_api_tests)

🔹 Що робить

Піднімає віртуальну машину на Ubuntu Встановлює Java та Maven Виконує команду mvn test (або mvn test -Dgroups=smoke, якщо передано параметр)

🔹 Для чого це потрібно: -Щоб тести автоматично перевірялися після кожного пушу; -Щоб можна було запустити лише певну групу тестів; -Щоб бачити статус тестів без локального запуску;

Щоб запускати лише певну групу з GitHub Actions, можна вказати:

- name: Run only smoke tests
  run: mvn -B test -Dgroups=smoke
  Структура проєкту
  src/test/java/... — тестові класи (PlaceholderAPIPostsTest, PlaceholderAPIUsersTest, PlaceholderAPICommentsTest)

src/test/java/org/example/helpers — хелпери для роботи з API (Posts, Users, Comments)

src/test/java/org/example/models — моделі (Post, User, Comment), що відправляються у запитах

BaseTest — базовий клас із налаштуванням RestAssured (baseURI, логування запитів і відповідей)